# Expected-fail baseline for the guard-core-rs conformance runner against spec 4.0.2.
# Generated from a real run of the current engine (preprocessing + semantic analysis only).
#
# Why so many xfails: the 4.x reference detect pipeline runs the full regex pattern table
# across four scan views; the Rust engine has no pattern table yet, so every case expecting
# regex threats cannot pass. A few cases also pin preprocessor divergences with tailored
# reasons below.
#
# Drift semantics (fail-closed, enforced by tests/conformance.rs and src/report.rs):
#   - failing case NOT listed here -> gate fails (unbaselined failure)
#   - listed case that now PASSES  -> gate fails (stale baseline; remove the entry)
#   - failing case listed here     -> xfail (tolerated, reported four-way)
spec_version = '4.0.2'
cases = [
    { case = 'inclusion_sensitive_recon::incl_data_uri', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::incl_expect_scheme', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::incl_phar_stream', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::incl_php_filter', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::incl_remote_http', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::incl_zip_stream', reason = 'preprocessor divergence: engine strips # and -- markers to end of line; the 4.x reference replaces the marker with a space; rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_actuator', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_joomla_installer', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_phpmyadmin', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_server_status', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_wp_admin', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::recon_wp_login', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::sensitive_aws_credentials', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::sensitive_config_bak', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::sensitive_env_file', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::sensitive_git_config', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'inclusion_sensitive_recon::sensitive_htpasswd', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::code_injection_eval_request', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::code_injection_system_diagnostics', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::deser_java_base64', reason = 'preprocessor divergence: base64 candidate decode is gated on printable ASCII; the 4.x reference decodes anyway and control-byte removal shortens the content; rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::deser_php_object', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::deser_pickle_reduce', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::file_upload_double_ext', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::file_upload_php_ext', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::http_split_crlf_cookie', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::ldap_injection_password', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::ldap_injection_uid', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::log4shell_jndi_dns', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::log4shell_jndi_ldap', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::log4shell_jndi_obfuscated', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::nosql_gt_empty', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::nosql_ne_null', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::nosql_where_sleep', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::proto_pollution_bracket', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::proto_pollution_dunder', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::template_asp_eval', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::template_dollar_brace', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::template_jinja_expr', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::xml_xxe_file', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'misc_injection::xml_xxe_remote', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_absolute_etc', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_deep_padding', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_dots_repetition', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_double_backslash', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_double_dot_slash', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_double_encoded', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_etc_shadow', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_proc_self_env', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_url_encoded', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'path_traversal::trav_windows_ini', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'semantic::sem_regex_plus_semantic_below_threshold', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'semantic::sem_structural_dense', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_benchmark', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_comment_bypass', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_double_query', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_drop_table', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_information_schema', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_inline_comment_obfuscation', reason = 'preprocessor divergence: engine removes inner-word SQL block comments; the 4.x reference preserves them via a lookbehind-guarded strip; rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_load_file', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_or_string_compare', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_or_tautology', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_pg_sleep', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_sleep', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_sqlite_version', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_union_password', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_union_select', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_update_set', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_url_encoded_quote', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_waitfor_delay', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'sqli::sqli_xp_cmdshell', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_anchor_href_data', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_body_onload', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_broken_tag_escape', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_details_ontoggle', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_encoded_script', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_event_handler_nested_quote', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_form_action', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_html_entities', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_iframe_javascript', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_img_onerror', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_input_autofocus', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_javascript_uri', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_marquee_onstart', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_mixed_case', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_object_data', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_script_alert', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_select_onchange', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_src_doc', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_style_expression', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_svg_onload', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
    { case = 'xss::xss_video_source', reason = 'rust engine lacks the 4.x pattern-table scan stage: regex threats are not produced' },
]
